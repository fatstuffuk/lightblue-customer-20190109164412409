server:
  context-path: /micro
  port: 8080

spring:
  cloudant:
    username: ${vcap.services.lightblue-cloudiant.credentials.username}
    password: ${vcap.services.lightblue-cloudiant.credentials.password}
    host: ${vcap.services.lightblue-cloudiant.credentials.host}
    protocol: https
    port: 443
database: customers